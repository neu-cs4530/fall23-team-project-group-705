/**
 * This is a terrible way to store the wordlist, but I spent multiple days trying
 * and failing to get JSON imports to work, so it will do for now.
 * 
 * @returns a list of all of the possible words for a pictionary game, in an array.
 */
export default function PictionaryWordlist(): string[] {
  return (
    [
      "A",
      "B",
      "C",
      "D",
      "E",
    ]
  );
}